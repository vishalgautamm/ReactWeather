var React = require('react');

var Examples = (props) => {
    return (
        <div>
            <h1>Examples</h1>
            <p> Welcome to examples page!</p>
        </div>
    )};

module.exports = Examples;